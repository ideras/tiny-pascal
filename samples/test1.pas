{ 
  *********************************
  * File: test1.pas 
  * A test program for expressions 
  *********************************
}
program test1;
begin
    x := (10 - 3);
    y := (x * 10) div 5;

    //Imprimir el resultado
    writeln (x + y) * 30;
end.